## 八字渲染prompt

请将我上传的这张八字排盘图片，严格按照视觉样式还原成一份可在支持 HTML/CSS 的 Markdown 环境中渲染的代码。

**具体要求如下：**

1. 使用 `<div>` + `<table>` 结构，包含“流年、大运、年柱、月柱、日柱、时柱”六列；
2. 表格分为三行：
   - 第一行：十神（如“食神”“正财”等），字体小号（约14px），灰色或深灰；
   - 第二行：天干地支，天干与地支上下排列，字号大（约28px），加粗，颜色需严格匹配原图，颜色映射关系如下
     壬、癸、子、亥：007bff
     戊、己、辰、戌、丑、未：8B6D03
     甲、乙、寅、卯：00cc66
     丙、丁、巳、午：d63031
     庚、辛、酉、申：ff8c00
   - 第三行：藏干十神，背景白色，顶部有浅灰分隔线(border-top: 1px solid #ddd)，文字颜色按对应十神着色（如“辛偏印”用橙色，“乙食神”用绿色，“癸比肩”用蓝色等）；
3. 在“大运”列与“年柱”列之间添加一条清晰的竖线分隔符（建议使用 `border-left: 2px solid #ddd;` 样式），以视觉上区分运势与命局主体部分；
4. 整体容器带浅灰背景（#f9f9f9）、圆角边框(border-radius: 12px)、内边距(padding: 20px)，居中显示（margin: 20px auto; max-width: 600px）；
5. 所有颜色、字号、对齐方式、间距均需尽量贴近原图，做到“所见即所得”；
6. 若无法识别颜色，请标注“待确认”，并说明推测依据；
7. 在表格最底部增加一行，内容为“命理1”，居中显示，字号16px，加粗，深灰色（`#333`）；*
8. 最终输出仅包含完整 HTML + CSS 代码块，无需解释或注释。

请开始处理我上传的图片。

